module.exports = args => {
    console.log('Storybook-AEM Init\n', args);
    /*
        # Questions
        1. Where to install storybook folder 
        2. Where do components live - './src'
        3. Where will stories live - './src' alongside components
        4. What JS framework will you use - preact/react
    */
}